import { Module } from './models/index.js';


// eslint-disable-next-line @typescript-eslint/no-unused-vars,@typescript-eslint/no-empty-function
export function link(_moduleDocs: Module[]): void {
    // TODO(Jordi M.): Link Phase.
    //  - Cross-reference any user defined type/interface.
    //  - Build the inheritance relationship.
    //  - Also check if the default value of a declaration is a variable and resolve it's value.
}
